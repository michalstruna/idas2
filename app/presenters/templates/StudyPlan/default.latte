{block content}

<section class="item__list">
    <header class="item__list__header">
        <h1 n:block="title" class="item__list__title">
            Studijní plány ({count($studyPlans)})
        </h1>

        {control tabMenu, $tabs}
    </header>

    {control table, $studyPlans, $isAdmin, ['nazev', 'Název'], ['obor', 'Obor'], ['odhad_studentu', 'Odhad počtu studentů'] }
</section>